[package]
name = "pgraph"
version = "0.1.0"
edition = "2021"
description = "Positioned graph: minimal data contract (positions + edges + index containers)"

[dependencies]
numcore = { workspace = true }
