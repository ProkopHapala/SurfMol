[package]
name = "pgraph"
version = "0.1.0"
edition = "2021"
description = "Reusable graph algorithms: adjacency, components, bridges, loops, reorder, selection, picking. Operates on numtypes data contracts."

[dependencies]
numtypes = { workspace = true }
