---
type: folder
title: crates
description: Cargo workspace members — library crates in libs/, binary crates in apps/.
tags: [rust, workspace]
timestamp: 2026-08-25
---

# crates

Cargo workspace members. Split into:

- **`libs/`** — 10 library crates (no binary targets, only `lib.rs`)
- **`apps/`** — 4 binary crates (standalone executables)

See the READMEs in each subfolder for the crate index and dependency graph.
